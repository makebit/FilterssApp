<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingStart="32dp"
    android:paddingBottom="8dp">

    <!--
    <TextView
        android:id="@+id/exp_menu_group_item_count"
        style="@style/material_type_scale_subtitle2"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_alignParentTop="true"
        android:layout_gravity="right|center"
        android:background="@drawable/bg_grey"
        android:gravity="center"
        android:text="0"
        android:textColor="#000000"
        android:textSize="8dp" />-->

    <ImageView
        android:id="@+id/exp_menu_group_item_icon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="end"
        app:srcCompat="@drawable/ic_rss_feed_black_24dp" />

    <TextView
        android:id="@+id/exp_menu_group_item"
        style="@style/material_type_scale_caption"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginStart="8dp"
        android:gravity="start|center"
        android:maxLines="1"
        android:text="Menu Item" />

</LinearLayout>